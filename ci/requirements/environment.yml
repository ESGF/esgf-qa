name: esgf-qa-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cftime
  - cf_xarray
  - dask
  - netCDF4
  - packaging
  - pandas
  - textual
  - xarray
  - pip
  - pip:
      # Required CI uses published releases matching the project metadata.
      - compliance-checker>=6.1.0
      - cc-plugin-cc6>=0.4.3
      - cc-plugin-wcrp>=2.3.0
      - pytest>=9,<10
      - pytest-asyncio
      - pytest-cov
